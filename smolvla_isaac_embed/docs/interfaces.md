# 接口约定

本文件用于描述 `Isaac Sim / IsaacLab` 与 `SmolVLA` 之间最关键的输入输出接口。

这不是泛泛介绍，而是项目当前真实使用的接口约定。

## 1. 总体链路

```text
Isaac 环境观测
-> 观测适配层
-> LeRobot / SmolVLA 输入
-> SmolVLA 输出动作
-> 动作适配层
-> Isaac 环境执行
```

## 2. 环境观测原始结构

记录 Isaac 环境实际返回的 observation 结构。

### 2.1 顶层字段

- `policy`：
- `camera_obs`：
- 其他：

### 2.2 `policy` 子字段

- `robot_joint_pos`：
- 其他：

### 2.3 `camera_obs` 子字段

- `robot_pov_cam_rgb`：
- 其他：

## 3. LeRobot / SmolVLA 期望输入

### 3.1 状态输入

- 目标字段名：`observation.state`
- 来源字段：
- 拼接顺序：
- 最终维度：

### 3.2 图像输入

- 目标字段名：
- 来源字段：
- 图像尺寸：
- 通道顺序：
- 是否归一化：

### 3.3 任务文本

- 输入字段名：`task`
- 文本来源：
- 当前示例任务：

## 4. `state_keys`

当前配置：

```text
待填写
```

## 5. `camera_keys`

当前配置：

```text
待填写
```

## 6. `rename_map`

当前配置：

```json
{}
```

说明：

- 左侧是环境当前名字
- 右侧是策略期望名字

## 7. 动作接口

### 7.1 模型输出

- 动作张量形状：
- `action_dim`：
- 是否 chunked action：

### 7.2 动作语义

记录每一维动作对应的控制含义。

| 索引 | 名称 | 含义 | 备注 |
|---|---|---|---|
| 0 | 待填写 | 待填写 | 待填写 |

### 7.3 环境执行顺序

动作在送回环境前是否需要：

- 重排
- 裁剪
- 限幅
- 类型转换

## 8. 当前最大风险点

优先记录最容易出错的接口问题。

- 图像命名不一致
- 状态拼接顺序不一致
- 动作维度不一致
- task 文本缺失
